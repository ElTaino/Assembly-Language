.intel_syntax noprefix
.global _start
.text
_start:

;# for loop 1-10
mov rax, 0
compare:
cmp rax, 10
jge exit
inc rax
jmp compare

exit:
;#breakpoint
 int3
 mov rax, 60
 xor rdi, rdi
 syscall

print:
 mov rax, 1
 mov rdi, 1
 lea rsi, [s1]
 lea rdx, [s1_len]
 syscall
 jmp exit

.data
 s1: .ascii "less than!"
 s1_len = . - s1


;# SYSCALL(WRITE, TERMINAL, addr, len);
;# WRITE(TERMINAL, addr, len);

;# system call code: RAX
;# Parameter Order:
;# RDI
;# RSI
;# RDX
;# RCX
;# R8
;# R9

